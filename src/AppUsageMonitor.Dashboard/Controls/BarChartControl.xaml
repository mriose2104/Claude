<UserControl x:Class="AppUsageMonitor.Dashboard.Controls.BarChartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="420">
    <ItemsControl x:Name="ItemsHost">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Margin="0,6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Etiqueta}" FontSize="13"
                                   Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="1" Text="{Binding DuracionTexto}" FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0" />
                    </Grid>
                    <Border Grid.Row="1" Height="10" CornerRadius="5" Background="#E5E7EB"
                            HorizontalAlignment="Left" Width="260" Margin="0,4,0,0">
                        <Border CornerRadius="5" Background="{StaticResource BrandBrush}"
                                HorizontalAlignment="Left" Width="{Binding AnchoBarra}" />
                    </Border>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
